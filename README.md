# Cursor-Unity-Rules-Workflow

### 🎮 基于演化节点的 Unity AI 自动化开发流配置

> **工程化思维——“用规则约束 AI，用文档管理状态，用接口解耦系统”**
> **真正落地的 Vibe Coding：不仅仅是 Prompt，而是工业化的高效虚拟团队。** 

## 💡 灵感来源 (Credits & Inspiration)

本项目的核心理念（演化节点与流水线思维）深度借鉴了 **B站 UP 主 @AI-Ghost-Lab** 的 流水线思想。

我在此基础上，针对 **Unity 游戏开发** 的特殊性（组件化、生命周期、重资产）进行了深度适配与重构，将其转化为适配 **Cursor ** 的交互式配置。

> **致敬原作者 / Special Thanks to:** 🎥 [**AI时代下，如何放大自己的能力到10倍甚至百倍_哔哩哔哩_bilibili**](https://www.bilibili.com/video/BV1ktUgB9Evp/) 👤 **@AI-Ghost-Lab**

## ⚠️ 核心哲学：接口先行 

**这不是拯救“屎山代码”的魔法棒。**

这套工作流的底层逻辑是**“契约式编程”**。为了最大化发挥 AI 的效能并避免上下文溢出，你的项目应当遵循 **解耦架构。你可以和架构师探讨如何建立或者重构一个解耦架构。

### 最佳实践 (Best Practices)

1. **人类定义契约 :** 你负责编写 `Interfaces` (如 `IDamageable`, `IInteractable`) 或抽象基类。这是你对系统边界的控制。
2. **AI 实现细节:** 通过演化节点（Evolution Node）锁定状态，让 AI 填充具体的逻辑实现。
3. **禁止强耦合:** 如果你的代码充斥着跨脚本的直接引用（Direct References），AI 将很难理解上下文。请优先使用 **事件总线 (Event Bus)** 或 **依赖注入 (DI)**。

> **“不要让 AI 猜测你的架构。你搭建骨架，AI 填充血肉。”**

## 🤖 虚拟工作室架构 (Virtual Studio Architecture)

这不仅仅是一组 Prompt，这是一个驻扎在你 IDE 里的 **7人精英团队**。

通过 `.cursorrules` 的路由，AI 虽然会根据你的意图自动切换角色，但建议在对话中还是手动@角色。

| 角色 (Role)         | 代号 (Alias)             | 核心职责 (Core Responsibility)                               | 典型触发场景 (Trigger)                          |
| ------------------- | ------------------------ | ------------------------------------------------------------ | ----------------------------------------------- |
| **Project Manager** | `The Razor` (奥卡姆剃刀) | **需求砍单与进度控制**。负责垂直切片范围管理，拒绝一切对核心体验无用的功能。 | "我想加个背包功能...""每日进度检查"             |
| **Historian**       | `Time Keeper` (史官)     | **演化节点管理**。维护 `_Evolution` 目录，创建 `manifest.md`，确保项目上下文不丢失。 | "新建一个任务...""复盘一下之前的改动"           |
| **Architect**       | `Blueprinter` (架构师)   | **系统设计与账本规划**。编写 `ledger.json` (施工账本)，规划技术方案，**不写代码**。 | "如何设计武器系统？""这个功能怎么解耦？"        |
| **Tech Designer**   | `Pipeline Guard` (技策)  | **管线与数据定义**。定义 `ScriptableObject` 数据结构，编写 Editor 工具，连接策划与程序。 | "配置一下敌人数据...""写个编辑器工具"           |
| **Coder**           | `Executor` (工程师)      | **代码执行**。只读取 `ledger.json` 干活，严禁越权设计，严格遵守 SOLID 和 P0 规范。 | "开始写代码...""修复这个 Bug"                   |
| **QA**              | `Gatekeeper` (质检员)    | **质量审查**。检查 P0 红线（如单例、硬编码），生成审查报告，确保架构合规。 | "帮我 Review 一下代码""检查潜在 Bug"            |
| **Mentor**          | `Translator` (导师)      | **(个人需求)后端概念翻译**。用 Java/Spring 的概念解释 Unity 机制，辅助后端转行开发者。 | "ServiceLocator 是啥？""Unity 里的 Bean 是谁？" |

## 🔄 标准工作流 （SOP）

一个典型的功能开发闭环如下：

1. **需求阶段**: 用户提出想法 ： **@Historian** 创建演化节点,**@PM** 审查范围（Pass/Kill，可跳过）。
2. **设计阶段**: **Architect** 拆解需求并生成备选方案，生成节点文件夹 `ledger.json` (施工图)， **TD** 定义数据结构。
3. **执行阶段**: **Coder** 读取`ledger.json`账本， 编写 `C#` 代码  更新账本状态。
4. **验收阶段**: **QA** 审查代码 提出 P0/P1 问题 **Coder** 修复完成。



## 🚀 快速开始 

### 1. 安装配置 

1. 下载本仓库。
2. 将 `.cursor/` 文件夹和`.cursorrules`文件完整复制到你的 Unity 项目根目录，查看并按需修改`.cursorrules`里的基本信息。

### 2. 初始化演化流 

在 Cursor 的 Chat 中输入：

> "我想用开发一个新功能。 @historian"

### 3. 开始 Vibe Coding

AI 将引导你完成以下闭环：

- **Manifest:** 记录需求。
- **Ledger:** 架构师生成 JSON 施工图。
- **Coding:** 工程师读取 JSON 写代码。
- **Review:** QA 审查代码合规性。

## 🛡️ P0 级架构红线 (P0 Constraints)

本配置内置了针对 Unity 工业级开发的绝对防御机制：

- ❌ **No Singletons:** 严禁使用单例模式，强制使用 `ServiceLocator`。
- ❌ **No Instantiate:** 严禁直接 `new` 或 `Instantiate`，强制使用 `SpawnManager`。
- ❌ **No Hardcoding:** 严禁魔法数值，强制使用数据驱动 (`ScriptableObject`)。
- ❌ **No Spaghetti:** 强制 View（表现）与 Logic（逻辑）分离。

## 📂 目录结构推荐 (Recommended Structure)

为了配合这套工作流，建议你的 Unity 项目采用以下结构：

```
Assets/
├── _Evolution/          <-- [核心] 演化节点历史记录 (AI 自动维护)
│   ├── 001_Init/
│   │   ├── manifest.md
│   │   └── ledger.json
├── _Project/            <-- 你的游戏资产
│   ├── Core/            <-- 核心架构 (ServiceLocator, Events)
│   ├── Systems/         <-- 业务模块 (Weapon, Player, Enemy)
│   └── Configs/         <-- ScriptableObjects
```

##  🤝 贡献 (Contributing)

欢迎提交 PR 或 Issue 分享你的 `.mdc` 角色配置。

License: MIT
